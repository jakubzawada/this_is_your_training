import 'package:flutter/material.dart';

class SundayPageContent extends StatelessWidget {
  const SundayPageContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Sunday')),
    );
  }
}
