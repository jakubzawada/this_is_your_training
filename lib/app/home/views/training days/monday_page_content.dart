import 'package:flutter/material.dart';

class MondayPageContent extends StatelessWidget {
  const MondayPageContent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Monday')),
    );
  }
}
